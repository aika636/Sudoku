# Разработка

## Текущее состояние

**Готовы Фазы 0–2** (скелет, ядро, доска и окно). Подробности и критерии — `docs/roadmap.md`.

| Фаза | Состояние |
|---|---|
| 0. Скелет и деплой | ✅ загружается в ST, панель настроек в drawer |
| 1. Ядро (солвер, генератор, оценка сложности) | ✅ 400 головоломок в тестах, все валидны и уникальны |
| 2. Доска, окно, точки запуска | ✅ код и jsdom-тесты; **в браузере глазами не проверено** |
| 3. Взаимодействие (ввод, заметки, undo, победа) | ⬜ следующая |
| 4. Персистентность и настройки | ⬜ модель и сериализация уже готовы (`game.js`), не подключены к `extensionSettings` |
| 5. Полировка (мобильные, темы, a11y) | ⬜ |

Что именно осталось в Фазе 3: клики по клеткам, ввод с клавиатуры и экранный numpad,
режим заметок, undo/redo (модель готова, нужны кнопки), счётчик оставшихся цифр,
детект победы. Главный неизвестный фактор — перехват хоткеев ST (см. ниже).

## Установка в SillyTavern

Тестовая инсталляция — docker на `home-services`, UI `http://10.10.10.20:8800`.
Каталог расширений — per-user, он же единственный на смонтированном томе:

```
/srv/dockers/sillytavern/data/default-user/extensions/Sudoku
```

`public/scripts/extensions/third-party` внутри контейнера использовать нельзя: он
обнуляется при обновлении образа.

```bash
./deploy.sh          # rsync рабочего дерева на home-services
```

Скрипт исключает `docs/`, `tests/`, `CLAUDE.md`, `.gitignore` и служебные каталоги —
в таверну уезжает только то, что грузит браузер. Внимание: rsync с `--delete` **не удаляет
исключённые файлы**, уже лежащие на хосте; если что-то попало туда раньше, убирать вручную.

После заливки — **хардрелоад вкладки** (Ctrl+Shift+R). Горячей перезагрузки расширений в ST нет.

## Тесты

Ядро — без зависимостей, свой раннер `tests/_harness.mjs`:

```bash
node --check index.js              # синтаксис без браузера
node tests/solver.test.mjs         # решатель + операции над сеткой (16 проверок)
node tests/generator.test.mjs      # генератор и сложность, 100 головоломок на уровень
node tests/generator.test.mjs 10   # быстрый прогон
node tests/game.test.mjs           # модель партии (22 проверки)
```

UI — под jsdom, единственная dev-зависимость:

```bash
npm install --no-save jsdom
node tests/ui.test.mjs             # доска, окно, точки запуска (14 проверок)
```

Асинхронные тесты в файлах пишутся как `await test('...', async () => {...})` — раннер
возвращает промис только для них, и не дождаться его значит запустить тесты внахлёст.

## Отладка

* Все логи расширения идут с префиксом `[Sudoku]` — фильтр в консоли по нему.
* При загрузке в консоли должно быть `[Sudoku] vX.Y.Z загружен` и
  `[Sudoku] панель настроек инициализирована`.
* Состояние партии живёт в замыкании `src/ui/modal.js`; чтобы посмотреть его руками,
  проще открыть окно и дёргать функции из `src/core/game.js` в консоли, импортировав модуль
  по URL расширения.
* Настройки: `SillyTavern.getContext().extensionSettings.Sudoku`.

## Чеклист ручной проверки

### Фаза 0–2 (текущая)

- [ ] После хардрелоада в консоли `[Sudoku] v0.2.0 загружен`, без ошибок.
- [ ] В Extensions drawer есть сворачиваемый блок «Sudoku» с четырьмя контролами.
- [ ] Кнопка «Судоку» появилась в меню волшебной палочки.
- [ ] Клик по кнопке открывает окно с доской 9×9; линии боксов толще внутренних.
- [ ] Подсказки видны, введённых цифр нет; таймер идёт.
- [ ] Смена уровня в селекторе генерирует новую доску и сохраняется в настройки.
- [ ] «Новая игра» выдаёт другую головоломку.
- [ ] Окно закрывается крестиком и по Esc; после закрытия открывается снова.
- [ ] Доска читаема в тёмной и светлой темах.
- [ ] `/sudoku` и `/sudoku expert` открывают игру.

### Фаза 3 (когда будет)

- [ ] Цифры с клавиатуры при открытом окне **не попадают в поле ввода чата**.
- [ ] Esc закрывает окно, а не только снимает выделение клетки.
- [ ] Головоломка проходится до конца только клавиатурой; отдельно — только мышью.
- [ ] Undo откатывает и авточистку пометок у соседей.

## Известные подводные камни

* **Хоткеи ST.** Таверна вешает глобальные обработчики клавиатуры. Обработчики игры
  должны жить только при открытом окне и звать `stopPropagation()`, иначе цифры уедут
  в поле ввода чата. Не проверено на живой инсталляции — Фаза 3.
* **rsync и исключения.** См. выше: `--delete` не трогает исключённые файлы.
* **Второе окно.** `openSudoku()` держит гвард `session`; две доски с одним состоянием
  разъехались бы.
